import 'package:flutter/material.dart';

class MembresScreen extends StatelessWidget {
  const MembresScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Membres')));
}
