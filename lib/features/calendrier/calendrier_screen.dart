import 'package:flutter/material.dart';

class CalendrierScreen extends StatelessWidget {
  const CalendrierScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Calendrier')));
}
