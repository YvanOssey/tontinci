import 'package:flutter/material.dart';

class CotisationScreen extends StatelessWidget {
  const CotisationScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Cotisations')));
}
